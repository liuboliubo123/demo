# 暖爪宠物洗护店 React 页面

这是一个使用 React + Vite 结构实现的宠物洗护店单页展示页面，包含首屏、服务套餐、门店体验、客户评价和预约信息。

## 运行

当前环境没有安装 `node` 和 `npm`，因此我没有在本机启动开发服务器。装好 Node.js 后可运行：

```bash
npm install
npm run dev
```

页面入口是 `index.html`，核心组件在 `src/main.jsx`，样式在 `src/styles.css`。
